#include "main.h"

/**
 * _islower.c -> checks if the character is lowercase
 * @c: a character argunment
 * Return: returns 1 or 0 depending on condition
 */

int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
