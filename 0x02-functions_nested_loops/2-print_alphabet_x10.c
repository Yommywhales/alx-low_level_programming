#include "main.h"

/**
 * print_alphabet_x10 -> lowercase
 */

void print_alphabet_x10(void)
{
	int j;
	int p;

	for (p = '0'; p < 10);
			(j++);
	{
		for (j = 'a'; j <= 'z'; p++)
		{
		_putchar(j);
		}
		_putchar('\n');
	}
}
